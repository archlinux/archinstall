# H8D13'S TESTING LAB

One liner for testing `
pacman-key --init && pacman -Sy --noconfirm git && git clone -b testing https://github.com/h8d13/archinstall-patch && cd archinstall-patch && python -m archinstall --debug --verbose 
`
